services:
  inference-mock:
    environment:
      INFERENCE_PROVIDER: local
      OLLAMA_URL: http://ollama:11434
      OLLAMA_VISION_MODEL: llava:7b
      OLLAMA_TEXT_MODEL: qwen2.5:1.5b
      OLLAMA_KEEP_ALIVE: 30m
      LOCAL_DRAW_URL: http://local-draw:7860/sdapi/v1/txt2img
      LOCAL_GUESS_URL: ""
      LOCAL_DRAW_STEPS: 14
      LOCAL_DRAW_CFG_SCALE: 6.5
      LOCAL_DRAW_WIDTH: 512
      LOCAL_DRAW_HEIGHT: 512
    depends_on:
      ollama:
        condition: service_started
      local-draw:
        condition: service_started
    volumes:
      - ./img-generated:/generated

  ollama:
    image: ollama/ollama:latest
    container_name: syncroom-ollama
    ports:
      - "${OLLAMA_PORT:-11434}:11434"
    volumes:
      - ollama_data:/root/.ollama
    networks:
      - syncroom-network
    restart: unless-stopped

  ollama-pull:
    image: ollama/ollama:latest
    depends_on:
      ollama:
        condition: service_started
    entrypoint:
      - /bin/sh
      - -c
      - |
        sleep 8
        ollama pull llava:7b
        ollama pull qwen2.5:1.5b
    environment:
      OLLAMA_HOST: http://ollama:11434
    networks:
      - syncroom-network
    restart: "no"

  local-draw:
    build:
      context: ./local-draw-service
      dockerfile: Dockerfile
    container_name: syncroom-local-draw
    ports:
      - "${LOCAL_DRAW_PORT:-7860}:7860"
    volumes:
      - hf_cache:/root/.cache/huggingface
      - ./img-generated:/generated
    networks:
      - syncroom-network
    restart: unless-stopped

volumes:
  ollama_data:
    driver: local
  hf_cache:
    driver: local
